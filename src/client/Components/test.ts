import {tcs, Component} from "@rbxts/tcs"

class test extends Component {
    static TAG = "test"

    constructor(root: Instance) {
        super(root)
    }

    public start() {
        
    }

    public destroy() {
        
    }
}

tcs.create_component(test)